import { MessagingDateFormatterPipe } from './messaging-date-formatter.pipe';

describe('MessagingDateFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new MessagingDateFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
